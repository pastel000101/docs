# 문서 관리 규약 — 지도

> 규약은 이 파일 하나가 아니다. **계기와 지도는 여기, 실행 규칙은 그 규칙이 쓰이는 자리에
> 있다** — 각 폴더의 CLAUDE.md 와 결정대장 머리말. 폴더의 파일을 만지면 그 폴더 규칙이
> 자동으로 딸려 온다. **장문 근거·반례·역사는 `docs/04_규약근거.md` 에 있다** — 같은 장
> 번호 아래 (★1차 이관 — 4·7장 근거는 아직 각 정본에★). ★정상 작업에서는 안 읽는다★ —
> 판정이 애매하거나 규칙끼리 충돌할 때 그 장만 연다.
> **장 번호는 파일을 넘나드는 안정 ID다** — "5장"은 어느 파일에 있든 5장이다 (지도는 1장 끝).
> 루트에는 상세를 다시 적지 않는다. **루트와 장 파일이 갈라지면 장 파일이 이긴다.**
> **폴더·파일 구조는 여기 없다** — `docs/00_구조.md` 가 정본이다.
> **★열려 있는 문제는 `README.md` 에 있다★** — 규약을 크게 손대기 전에 먼저 읽는다.
>
> 최종 확인: 2026-08-17
> 반증 조건: 없음 — 규칙은 합의다 (13장)
> 재론 조건: ★잠정을 안 열고 일을 시작한 적이 있을 때★ · 장 지도에 없는 규칙이 생겼을 때 ·
>            어느 장이 정본인지 몰라 두 곳을 다 고친 적이 있을 때 ·
>            근거(04)를 열었는데 실행에 아무 영향이 없던 일이 반복될 때

---

## 0. 무엇을 언제 만드나 — ★하기로 한 것은 무조건 잠정부터★

**사건은 파일 하나다. 그 파일이 세 폴더를 지나간다.** 폴더가 바뀌는 게 곧 진행 상태다.

```
잠정/YYYY-MM-DD_제목.md      계획 — 하기로 한 순간. ★예외 없다★ (3·5장)
   │   확정 → 원본/잠정/YYYY-MM/ 에 스냅샷 (16장)
   ▼   ★같은 파일이 이동한다 — 이름은 끝까지 안 바뀐다★
진행중/YYYY-MM-DD_제목.md    실행 — 계획 항목을 하나씩 처리하며 쌓는다 (14장)
   │   완료 → 원본/진행중/YYYY-MM/ 에 스냅샷 + '상시로 갈 것' 이관 (6·16장)
   ▼
종결/YYYY-MM/YYYY-MM-DD_제목.md   동결 — ★지우지도 고치지도 않는다★
   │   수정할 거리가 생겼다 → ★종결을 고치지 않는다★
   └──→ 잠정/ 에 새 계획 (다음 사이클 · 15장)
```

### ★쓰기 전에 읽는다★ — 규칙이 필요한 시점이 규칙이 도착하는 시점보다 앞선다

폴더 규칙은 그 폴더의 파일을 만질 때 딸려 온다. 그런데 문서를 처음 만드는 순간엔
아직 그 폴더를 만진 적이 없다. 그래서 아래 셋은 ★손대기 전에 그 파일을 먼저 연다★:

```
잠정 문서를 열기 전      → docs/잠정/CLAUDE.md    머리말 여섯 칸 · 계기 원문 · 비밀 치환 (3장)
                          여섯 칸 = 계기 · 하기로 한 것 · 확정 조건 · 종결 조건 · 가정 · 상시로 갈 것
상시 문서를 새로 만들기 전 → docs/CLAUDE.md        NN 이름 규칙 (2장) · 머리말 (13장)
회의 기록을 받은 즉시     → docs/회의록/CLAUDE.md  원본 보관 · 요약 금지 · 네 절 (7장)
                          네 절 = 결정 · 일단 이렇게 · 미결 · 액션
```

★칸 이름만 여기 둔다★ — 이름이라도 손에 있어야 빠뜨림을 눈치챈다. 상세는 그 파일이
정본이다. **규격 미달로 태어난 문서는 되돌릴 수 없다** — 확정 시점에 그대로 원본/에
박제된다 (04_규약근거.md 0장).

나머지는 계기가 생길 때 만든다:

```
사용자가 정해야 할 게 생겼다      → 03_결정대장.md ①절에 행 하나 (4장)
회의를 했다                      → 원본/회의/ + 회의록/ (7장)
"이건 끝나도 남는 지식"이 쌓였다  → docs 루트 NN_… 상시 문서 (머리말은 13장)
"고쳐야 하는데 지금은 아니다"     → NN_백로그.md — 생기면 그때 만든다
읽는 순서를 말하고 싶어졌다       → 00_구조.md 의 `## 읽는 순서` — ★번호를 옮기지 않는다★ (2장)
눈으로 점검이 안 된다             → 그때 스크립트 (11장)
```

### "하기로 했다" 의 문턱 — ★낮게, 그러나 0 은 아니다★

```
잠정 문서를 연다      여러 단계를 밟는 것 · 결과물이 남는 것 · 되돌리는 데 비용이 드는 것
그냥 한다             한 번에 끝나는 것 — 오타 수정 · 질문에 답하기 · 파일 하나 읽기
```

**애매하면 연다** — 무서운 것은 문서가 많아지는 게 아니라 ★끝난 뒤 회고 한 장만 남는
것★이다 (04_규약근거.md 0장). ★잠정 문서를 연다고 작업이 멈추지 않는다★ — 계획을
적고 바로 간다. 기다리는 것은 확정 조건에 사용자가 걸려 있을 때뿐이다 (5장).

### ★빈 것을 미리 만들지 않는다★ — 사건 파이프라인 ★밖에서만★

백로그·스크립트·항목 유형·월 폴더는 계기가 생길 때 만든다 — 미리 만든 빈 것은
거짓말을 한다 (04_규약근거.md 0장). ★사건 문서는 예외다 — 하기로 한 순간 만든다★
(여기에 억제를 걸면 관문 전체가 죽는다). `00_구조.md` 도 예외다 — 빈 게 아니라 태어날
때 내용이 있다. ★억제 대상은 빈 것이지 미리 아는 것이 아니다.★

### `잠정/` `진행중/` 은 ★차 있는 게 정상★이다

비어 있다는 것은 "지금 아무것도 하고 있지 않다"는 뜻이다 — 폴더를 열면 무엇을
계획했고 무엇을 실행 중인지 보여야 한다 (04_규약근거.md 0장).
**월별로 가르지 않는다** — 몇 건이 열려 있나가 한눈에 보여야 한다 (★2장 정본★).

## 1. 문서에는 세 가지 수명이 있다 — 폴더가 그걸 나눈다

| 종류 | 위치 | 성격 | 이름 |
|---|---|---|---|
| **상시** | docs 루트 `NN_…` | 갱신되지만 **완료되지 않는다** (지금의 지식) | `NN` = 안정 ID. ★이름 고정·이동 금지★ |
| **사건** | `잠정/` → `진행중/` → `종결/YYYY-MM/` | 계획 → 실행 → 동결. **한 파일이 지나간다** | `YYYY-MM-DD_제목.md` · ★태어난 날 · 끝까지 안 바뀐다★ |
| **원자료** | `원본/` + 읽기용 사본 `회의록/` | 더 안 바뀐다 — **수명이 하나뿐이다** | 원 날짜 유지 |
| **규칙** | 각 폴더의 `CLAUDE.md` | **파이프라인 밖** — 고칠 때만 움직인다 | `CLAUDE.md` 고정 · 폴더가 범위다 |

- ★잠정은 독립된 수명이 아니라 사건의 첫 시기다★ — 모든 사건이 반드시 거치는
  관문이다 (조건이 붙는 순간 아무도 안 지나간다 · 04_규약근거.md 1장).
- 수명을 셋으로 나누는 이유: 계획/실행/끝이 ★폴더 위치만으로★ 갈린다 (04_규약근거.md 1장).
- ★폴더 위치가 상태다★ — 머리말 표기(`[종결]` `[폐기]`)와 갈라지면 폴더가 이긴다.
  예외 하나: `종결/` 안에서는 머리말이 유일한 표기다 — 둘의 취급이 같아 폴더를 안
  쪼갠다 (04_규약근거.md 1장).
- ★`회의록/` 은 넷째 수명이 아니라 `원본/회의` 의 사본이다★ — 갈라지면 원본이 이긴다 (7장).
- 원자료와 그 사본만 내용으로 이름을 짓는다 — 나머지는 수명이 이름이다.
- 아카이브 폴더는 ★아직 없다★ — 물러날 파일이 생기면 그때 (00_구조 '아직 없는 것').

### ★규칙 문서가 사건이 되는 문턱★ — 0장의 문턱을 규약 자신에게도 적용한다

```
잠정 문서를 연다   장을 새로 만들거나 지운다 · 단계 번호를 옮긴다 · 폴더를 늘리거나 줄인다
그냥 고친다        문장 다듬기 · 오타 · 예시 추가 · 근거 보강 · 표현 통일
```

왼쪽은 전부 규약 안의 인용을 같이 고쳐야 하는 것들이고, 오른쪽은 되돌리면 그만이다
(왜 규칙이 파이프라인 밖인지는 04_규약근거.md 1장). **규칙에는 사건 문서의 `계기` 가
없다 — 그 대가로 ★한 줄 왜★를 규칙 옆에, ★장문 근거★를 04_규약근거.md 의 같은 장
번호에 둔다.** 한 줄 왜도 04 링크도 없이 규칙만 적으면 이 예외가 무너진다.

### ★실제 트리는 여기 없다 — `docs/00_구조.md` 가 정본★

무엇이 어디 있나는 00 이 답하고, 갈라지면 00 이 이긴다 — 00 은 반증 조건을 명령으로
갖는다 (04_규약근거.md 1장).

### 장 지도 — 각 장의 정본이 사는 곳

| 장 | 내용 | 정본 | 언제 읽히나 |
|---|---|---|---|
| 0 · 1 · 12 · 17 | 파이프라인 · 수명 · 단락 끝 · 도중 발동 | 이 파일 | 매 세션 |
| 2 · 8 · 9 · 10 · 11 · 13 | 이름 규칙 · 정본/사본 · 생성/소멸 · 이름 충돌 · 점검 · 상시 머리말 | docs/CLAUDE.md | docs 를 만질 때 |
| **3 · 5** | 사건의 탄생 · 계획 · 확정 | docs/잠정/CLAUDE.md | ★일을 시작할 때★ |
| **14 · 6** | 실행 누적 · 종결 절차 | docs/진행중/CLAUDE.md | 일하는 동안 · 닫을 때 |
| **15** | 동결과 재작업 사이클 | docs/종결/CLAUDE.md | 끝난 것을 또 고칠 때 |
| **16** | 스냅샷 적재 | docs/원본/CLAUDE.md | 확정·종결 시점에 |
| 4 | 결정 대장 사용법 | docs/03_결정대장.md 머리말 | 결정을 등재할 때 |
| 7 | 회의 기록 처리 | docs/회의록/CLAUDE.md | 회의가 들어올 때 |
| 18 | 근거 정본 사용법 — 정본 관계 · 부분 읽기 · 소멸 | docs/04_규약근거.md | 근거를 읽거나 지울 때 |

각 장의 ★장문 근거·반례·역사★는 `docs/04_규약근거.md` 의 같은 장 번호에 있다
(1차 이관: 0·1·2·3·5·6·8·9·10·11·13·14·15·16장 — 4·7장 근거는 아직 각 파일 안).

**번호는 순서가 아니라 안정 ID다** — 빈 자리가 생겨도 재사용하지 않고 (2장),
새 장은 언제나 **뒤에 붙인다.**

## 17. 단락 끝까지 기다리지 않는 것들 — 발동 시점이 작업 도중이다

```
"이걸 하기로 했다" 는 순간            → ★잠정 문서를 연다★ (3·5장) —
                                       계기(사용자 말 ★원문 그대로★·요약 금지) · 하기로 한 것 ·
                                       확정 조건 · 종결 조건 부터 적는다
계획이 확정된 순간                    → 스냅샷 + 진행중/ 으로 이동 (5·16장)
계획 항목 하나를 처리한 순간          → 진행중 문서에 그 자리에서 결과 한 줄 (14장)
계획에 없던 것을 하게 된 순간         → ★계획을 고치지 않는다★ 진행중에 '계획 밖' 으로 적는다 (14장)
작업이 끝나도 남을 지식이 나온 순간   → 그 문서 '상시로 갈 것' 에 그 자리에서 한 줄 (14장)
"사용자가 정해야 한다"고 판단한 순간  → 결정대장 ①절에 ★행 하나만★ 넣고 작업을 잇는다 (4장)
                                       단, 지금 막힌다(답 없이 다음 단계 불가)면 → ★바로 묻는다★
회의 기록이 들어온 순간               → 원본 보관 + 회의록 정리, ★그 즉시★ (7장)
```

## 12. 단락이 끝나면

```
1. 먼저 3줄로 정리한다 — 확정된 것 / 가설로 남은 것 / 다음으로 넘기는 것
   ★이 3줄이 안 써지면 아직 단락이 안 끝난 것★ — 갱신하지 않고 작업을 잇는다
2. 잠정/ 을 본다 — 확정된 것이 있으면 스냅샷 + 진행중/ 으로 (5·16장)
3. 진행중/ 을 본다 — 계획 항목이 다 처리됐나 · 종결 조건 충족 · '상시로 갈 것' 적립분
4. 종결 조건이 찼으면 종결 절차 (6장) · ★순서가 중요하다★
5. 결정대장 ①절의 `한 줄` 을 `상세` 로 · '막히는 것'이 사라진 행은
   ★사건 문서의 `가정` 칸을 먼저 정리하고★ 지운다 (4장)
6. 만진 상시 문서의 '최종 확인' 날짜 갱신
7. 사용자에게 3줄 보고 (갱신한 것 / 안 고친 것 / 다음으로 넘긴 것)
```

**해당 없으면 아무것도 안 고친다** — 억지로 적은 한 줄이 다음 사람을 오도한다.
★17장은 여기까지 기다리지 않는다 — 그 순간에 한다.★

**이 규약은 전부 사람(또는 모델)의 판단이다.** 자동으로 도는 건 루트가 매 세션, 폴더
규칙이 그 폴더를 만질 때 읽히는 것뿐이다. 그래서 **"지금 막힌 거 없어?"** 한마디가
유일한 안전망이다.
