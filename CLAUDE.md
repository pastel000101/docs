# 문서 관리 규약 — 지도

> 규약은 이 파일 하나가 아니다. **계기와 지도는 여기, 상세 절차는 그 절차가 쓰이는 자리에 있다** —
> 각 폴더의 CLAUDE.md 와 결정대장 머리말. 폴더의 파일을 만지면 그 폴더 규칙이 자동으로 딸려 온다.
> **장 번호는 파일을 넘나드는 안정 ID다** — "5장"은 어느 파일에 있든 5장이다 (지도는 1장 끝).
> 루트에는 상세를 다시 적지 않는다. **루트와 장 파일이 갈라지면 장 파일이 이긴다.**
>
> 최종 확인 2026-08-12 · 1장 트리와 실제 `docs/` 가 다르면 이 문서가 낡은 것이다.

---

## 0. 무엇을 언제 만드나 — ★빈 것을 미리 만들지 않는다★

폴더와 그 규칙 파일은 이미 있다 (`아카이브/` 만 필요해지면 그때). 그 안의 **문서**는 아래 계기가 생길 때 만든다.
아래는 만드는 순서가 아니라 **만드는 계기**다.

```
조사·작업이 하나 열렸다          → 진행중/ 에 문서 하나 (3장 · 안 만드는 게 기본 — 9장)
사용자가 정해야 할 게 생겼다      → 01_결정대장.md ①절에 행 하나 (4장)
결정 없이 가정을 깔고 진행했다    → 잠정/ 에 방침 문서 (5장)
"고쳐야 하는데 지금은 아니다"     → NN_백로그.md — 생기면 그때 만든다 (할 일 목록 · 지식이 아니다)
회의를 했다                      → 원본/회의/ + 회의록/ (7장)
사건이 끝났다                    → 종결 절차 (6장) 를 밟아 종결/YYYY-MM/ 으로
"이건 끝나도 남는 지식"이 쌓였다  → docs 루트 NN_… 상시 문서 (머리말은 13장)
문서가 10개를 넘었다              → 00_INDEX.md (읽는 순서·전체 지도)
눈으로 점검이 안 된다             → 그때 스크립트 (11장)
```

**빈 것을 미리 만들지 않는다**: 폴더는 각자 규칙 파일을 담고 있어 미리 있지만,
문서·INDEX·백로그·스크립트·항목 유형은 계기가 생길 때 만든다. 미리 만든 빈 것은
거짓말을 한다 — 사실은 "없다"가 아니라 "아직 쓸 일이 없었다"이다.
`진행중/` `잠정/` 은 ★비어 있는 게 정상★이고, 비어 있음이 곧 "열린 것 없음"이라는 정보다.

**코드 작업은 그 자체로는 계기가 아니다.** 버그 수정·리팩터·기능 구현은 문서 없이 그냥 한다.
위 표의 계기가 실제로 생겼을 때만 문서가 태어난다 — 작은 수정마다 사건 문서를 여는 순간
규약이 일보다 커진다. **이 규약은 문서를 다루는 규칙이지, 코드 작업의 허가 절차가 아니다.**

## 1. 문서에는 네 가지 수명이 있다 — 폴더가 그걸 나눈다

| 종류 | 위치 | 성격 | 이름 |
|---|---|---|---|
| **상시** | docs 루트 `NN_…` | 갱신되지만 **완료되지 않는다** (지금의 지식) | `NN` = 안정 ID. ★이름 고정·이동 금지★ |
| **사건** | `진행중/` → `종결/YYYY-MM/` | 시작과 끝이 있다 (조사·진단·작업) | `YYYY-MM-DD_제목.md` |
| **잠정** | `잠정/` | 죽지만 **죽일 조건이 자기 밖에 있다** (결정 대기 중의 방침) | 이름 고정 · 날짜 없음 |
| **원자료** | `회의록/` `원본/` `아카이브/` | 더 안 바뀐다 — **수명이 하나뿐이다** | 원 날짜 유지 |

**왜**: "진행중/완료"로만 나누면 대부분의 문서가 영원히 "진행중"에 남아 폴더가 아무것도
구분하지 못한다. 실제로 갈리는 축은 **날짜가 아니라 수명**이다.

**원자료 폴더만 내용으로 이름을 짓는다.** 나머지는 수명이 이름이다 —
가를 수명이 없는 곳에서만 내용 이름이 허용된다.

```
docs/
  CLAUDE.md                ← docs 공통 규칙 (2·8·9·10·11·13장 정본)
  00_INDEX.md              ← 문서 10개 넘으면. 그전엔 없다
  01_결정대장.md           ← 4장 정본 · 사용법은 그 머리말
  02_…  03_…               ← 상시 (NN_영역_이름.md · 생기면 그때)
  진행중/YYYY-MM-DD_제목.md               ← 3·6장은 그 폴더 CLAUDE.md
  종결/YYYY-MM/YYYY-MM-DD_제목.md
  잠정/방침_이름.md                        ← 5장
  회의록/YYYY-MM/YYYY-MM-DD_회의명.md      ← 7장 · 읽는 곳. 정리본 + 머리말
  원본/회의/YYYY-MM/YYYY-MM-DD_회의명.md   ← 원자료 · 받은 그대로
  아카이브/                                ← 필요해지면 그때
```

### 장 지도 — 각 장의 정본이 사는 곳

| 장 | 내용 | 정본 | 언제 읽히나 |
|---|---|---|---|
| 0 · 1 · 12 | 계기 · 수명 · 단락 끝 | 이 파일 | 매 세션 |
| 2 · 8 · 9 · 10 · 11 · 13 | 이름 규칙 · 정본/사본 · 생성/소멸 · 이름 충돌 · 점검 · 상시 머리말 | docs/CLAUDE.md | docs 를 만질 때 |
| 3 · 6 | 사건의 탄생 · 종결 절차 | docs/진행중/CLAUDE.md | 사건을 열고 닫을 때 |
| 4 | 결정 대장 사용법 | docs/01_결정대장.md 머리말 | 결정을 등재할 때 |
| 5 | 잠정 방침 | docs/잠정/CLAUDE.md | 가정을 깔고 진행할 때 |
| 7 | 회의 기록 처리 | docs/회의록/CLAUDE.md | 회의가 들어올 때 |

## 단락 끝까지 기다리지 않는 것들 — 발동 시점이 작업 도중이다

```
사건 문서가 태어나는 순간             → 머리말 세 칸부터 적는다 (3장) —
                                       계기(사용자 말 ★원문 그대로★·요약 금지) · 종결 조건 · 상시로 갈 것
작업이 끝나도 남을 지식이 나온 순간    → 그 사건 문서 '상시로 갈 것'에 그 자리에서 한 줄 (3장)
"사용자가 정해야 한다"고 판단한 순간   → 결정대장 ①절에 ★행 하나만★ 넣고 작업을 잇는다 (4장)
                                       단, 지금 막힌다(답 없이 다음 단계 불가)면 → 쌓지 말고 ★바로 묻는다★
회의 기록이 들어온 순간               → 원본 보관 + 회의록 정리, ★그 즉시★ (7장)
```

## 12. 단락이 끝나면

```
1. 먼저 3줄로 정리한다 — 확정된 것 / 가설로 남은 것 / 다음으로 넘기는 것
   ★이 3줄이 안 써지면 아직 단락이 안 끝난 것★ — 갱신하지 않고 작업을 잇는다
2. "무슨 일이 있었나 → 어디에 쓰나" 를 훑는다
3. 결정대장 ①절의 ⬜ 를 ✅ 로 · '막히는 것'이 사라진 행은 지운다
4. 진행중 문서의 종결 조건 충족 여부 · '상시로 갈 것' 적립분 확인
5. 가정을 깔고 진행한 게 있으면 → 잠정 문서 (5장)
6. 만진 상시 문서의 '최종 확인' 날짜 갱신
7. 사용자에게 3줄 보고 (갱신한 것 / 안 고친 것 / 다음으로 넘긴 것)
```

**해당 없으면 아무것도 안 고친다.** 억지로 적은 한 줄이 다음 사람을 오도한다.
★위 "단락 끝까지 기다리지 않는 것들"은 여기까지 기다리지 않는다 — 그 순간에 한다.★

---

## 부록 — 이 규약이 전제하는 것

1. **문서 ≠ 코드 → 코드가 맞다.** 설정·구조 상태는 문서가 가장 빨리 낡는다.
   그래서 반증 조건은 되도록 **실행 가능한 명령**으로 쓴다 (13장).
2. **원자료는 고치지 않는다.** 날짜별 기록·수집 원본에 나오는 옛 이름은 **당시 사실**이다.
   번역이 필요하면 대조표를 따로 둔다.
3. **전부 사람(또는 모델)의 판단이다.** 자동으로 도는 건 루트가 매 세션,
   폴더 규칙이 그 폴더를 만질 때 읽히는 것뿐이고, 등재·갱신·점검은 아무도 강제하지 않는다.
   그래서 **"지금 막힌 거 없어?"** 한마디가 유일한 안전망이다.

## 부록 — 여기서 겪은 것

> 규칙은 사고를 겪어야 무거워진다. 아래는 **이 프로젝트에서 실제로 난 일만** 적는다.
> 다른 데서 들은 사례는 넣지 않는다 — 남의 사고는 다음 사람을 설득하지 못한다.

```
(아직 없음 — 사고가 나면 그때 항목을 만든다. 유형을 미리 정하지 않는다)
```
